@using FlightSchoolTss.Data.Entities;
<FSTable TItem="PlatformDto" FstssItem="BindPlatformDto">
    <FstssHeaderTemplate>
        <MudTh>Id</MudTh>
        <MudTh>Name</MudTh>
        <MudTh>Maintainer</MudTh>
        <MudTh>Active?</MudTh>
        <MudTh></MudTh>
    </FstssHeaderTemplate>
    <FstssRowTemplate>
        <MudTd DataLabel="ID">@context.Id</MudTd>
        <MudTd DataLabel="Name">@context.Name</MudTd>
        <MudTd DataLabel="Maintainer">@context.Maintainer</MudTd>
        <MudTd DataLabel="Active?">@context.IsActive</MudTd>
        <MudTd DataLabel="DELETE">
            <MudIconButton Icon="@Icons.Material.Filled.Delete" OnClick="@(() =>OnButtonClick(context))" Color="Color.Error" />
        </MudTd>
    </FstssRowTemplate>
</FSTable>

@code {

    [Parameter]
    public EventCallback<PlatformDto> OnPlatformSelect { get; set; }

    [Parameter]
    public EventCallback<PlatformDto> OnPlatformDelete { get; set; }

    public async void BindPlatformDto(PlatformDto vm)
    {
        await OnPlatformSelect.InvokeAsync(vm);
    }
    public async void OnButtonClick(PlatformDto vm)
    {
        await OnPlatformDelete.InvokeAsync(vm);
    }
}